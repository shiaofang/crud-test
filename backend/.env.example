# 数据库连接配置（复制本文件为 .env 并按需修改）
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=123456
DB_NAME=crud_demo

# 允许跨域访问的前端地址，多个用逗号分隔
CORS_ORIGINS=http://localhost:5173

# JWT 密钥（生产环境务必修改为随机长字符串）
JWT_SECRET=change-me-in-production
JWT_EXPIRE_MINUTES=10080
