# Docker Compose 本地开发用（复制为 .env 后修改）
DB_PASSWORD=123456
CORS_ORIGINS=http://localhost
JWT_SECRET=change-me-in-production

# Docker Compose 生产用（由 CI 写入 .env.deploy，也可手动维护）
# BACKEND_IMAGE=ghcr.io/<owner>/<repo>-backend:latest
# NGINX_IMAGE=ghcr.io/<owner>/<repo>-nginx:latest
